// Funkcja do rozwijania/zawijania sekcji ustawień
function toggleSettings() {
    var settingsList = document.getElementById("settingsList");
    
    // Przełączanie klasy 'open' na sekcji ustawień
    settingsList.classList.toggle("open");
}